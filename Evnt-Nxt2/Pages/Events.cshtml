@page
@model Evnt_Nxt2.Pages.EventsModel
@{
    <ul>
        @foreach (var Event in Model.EventList)
        {
                <li>
                <a asp-page="/Events/Details" asp-route-id="@Event.Name">@Event.Name</a>
                <a asp-page="/Genres" asp-route-id="@Event.Organizer">@Event.Organizer</a>
                <span>@Event.Date</span>
                </li>
        }
        </ul>
}
